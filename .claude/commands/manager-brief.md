Create or refresh a concise manager-ready summary from the tracker.

Use `CLAUDE.md` as the operating policy. Treat `$ARGUMENTS` as optional scope, time window, or audience notes.

## Workflow

1. Read `docs\tracker\impact-log.md`, `docs\tracker\promotion-case.md`, and `docs\tracker\current-focus.md`.
2. Summarize the strongest recent evidence under the four boss-valued themes:
   - net new Dataverse value
   - legacy capability and reliability progress
   - COGS reduction or control
   - team and people growth
3. Prefer concrete outcomes, leverage, and scope over activity lists.
4. Update `docs\tracker\promotion-case.md` if it is missing the right synthesized language.
5. Provide a short manager-ready brief in the response.
